# Agent Run Report

## Task Chosen

Add invariant reference validation for missing tree nodes and files.

## Why This Task

The latest prior run recommended checking malformed invariant references. Invariants guide agent boundaries, so stale `nodeIds` or `filePaths` can leave future agents trusting rules that point at missing tree memory.

## Files Changed

- `packages/core/src/validator.ts`
- `packages/core/src/validator.test.ts`
- `packages/cli/src/index.ts`
- `.abstraction-tree/concepts.json`
- `.abstraction-tree/files.json`
- `.abstraction-tree/invariants.json`
- `.abstraction-tree/tree.json`
- `.abstraction-tree/changes/scan.1777825627893.json`
- `.abstraction-tree/runs/2026-05-03-1227-agent-run.md`
- `.abstraction-tree/lessons/2026-05-03-1227-lesson.md`
- `.abstraction-tree/automation/loop-state.json`

The worktree was already dirty before this loop from prior validation loops, including validator/CLI edits, regenerated abstraction-memory files, scan records, and prior run/lesson files. This loop preserved that work and added only invariant reference validation plus this loop's required records.

## Abstraction Layer Affected

schema

## Result

partial

## Checks Run

- `git diff -- README.md docs packages/core/src packages/cli/src .abstraction-tree` before editing
- Targeted reads of `README.md`, `docs/`, `packages/core/src/`, `packages/cli/src/`, `.abstraction-tree/`, prior run reports, and prior lessons
- `git diff -- packages/core/src/validator.ts packages/core/src/validator.test.ts packages/cli/src/index.ts`
- `npm run lint --if-present` - passed; no lint script is configured
- `npm run build` - core and CLI compiled, app build failed in Vite with `spawn EPERM`
- `npm test` - failed because Node's test runner hit `spawn EPERM`
- `node packages/core/dist/validator.test.js` - passed
- `node packages/core/dist/context.test.js` - passed
- `node packages/core/dist/scanner.test.js` - passed
- `node packages/core/dist/treeBuilder.test.js` - passed
- `npm run atree:scan` - passed
- `npm run atree:validate` - passed
- `git diff --stat -- README.md docs packages/core/src packages/cli/src .abstraction-tree` after scan

## What Improved

`validateInvariants` now reports invariant `nodeIds` that do not exist in `tree.json` and invariant `filePaths` that are missing from the known file set. CLI validation passes scanned files plus existing ignored invariant file paths, so valid references to `.abstraction-tree/` memory files do not create false positives. A focused validator test covers missing node and file references.

## What Did Not Improve

Validation still does not check tree node `invariants` entries against `invariants.json`, malformed concept references, tree node confidence ranges, or change record schema shape.

## Mistakes / Risks

The first implementation validated invariant file paths only against `files.json`, which falsely rejected `.abstraction-tree/tree.json` and `.abstraction-tree/files.json` because scanner output intentionally ignores `.abstraction-tree/`. I fixed this by letting the CLI provide existing ignored invariant file paths as known paths.

The canonical full build and `npm test` commands remain blocked by sandbox subprocess restrictions. Direct compiled core tests passed, but they are not a full replacement for the canonical commands.

Because this loop built on an already dirty validator, CLI, and abstraction-memory state, the final diff against `HEAD` includes prior validation loops as well as this invariant reference validation change.

## Missing Context Discovered

Invariant `filePaths` are broader than `files.json`; they can legitimately point at project memory files that are intentionally omitted from scanner summaries.

## Tree Updates Needed

No further tree updates are needed. `npm run atree:scan` refreshed root abstraction memory and `npm run atree:validate` passed.

## Reusable Lesson

Invariant file-path validation should allow existing ignored memory files, not only files listed in `files.json`.

## Recommended Next Loop

Add focused validation that tree node `invariants` entries reference existing invariant IDs from `invariants.json`.

## Final Instruction

Complete exactly one loop, then stop.
