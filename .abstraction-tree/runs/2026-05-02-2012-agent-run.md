# Agent Run Report

## Task Chosen

Document a low-token protocol for bounded self-improvement loops.

## Why This Task

Recent reports showed that useful loop state already exists in `.abstraction-tree/runs/`, `.abstraction-tree/lessons/`, and `.abstraction-tree/automation/loop-state.json`. Future agents can avoid broad exploration by starting there, but `docs/AGENT_PROTOCOL.md` did not yet say that explicitly.

## Files Changed

- `docs/AGENT_PROTOCOL.md`
- `.abstraction-tree/concepts.json`
- `.abstraction-tree/files.json`
- `.abstraction-tree/invariants.json`
- `.abstraction-tree/tree.json`
- `.abstraction-tree/changes/scan.1777767166654.json`
- `.abstraction-tree/runs/2026-05-02-2012-agent-run.md`
- `.abstraction-tree/lessons/2026-05-02-2012-lesson.md`
- `.abstraction-tree/automation/loop-state.json`

`package.json`, `packages/core/src/validator.ts`, `packages/core/src/validator.test.ts`, earlier `.abstraction-tree` scan records, `.abstraction-tree/evaluations/`, `.abstraction-tree/automation/active-loop.log`, `.abstraction-tree/automation/codex-loop-prompt.md`, and earlier run/lesson files were already dirty or untracked before this loop. The abstraction JSON files were also already dirty before this loop and were refreshed again by `npm run atree:scan`.

## Abstraction Layer Affected

docs

## Result

partial

## Checks Run

- `git diff --stat` before editing
- Targeted reads of `README.md`, `docs/`, `packages/core/src/`, `packages/cli/src/`, `.abstraction-tree/`, prior run reports, and prior lessons
- `git diff -- docs/AGENT_PROTOCOL.md`
- `npm run lint --if-present` - passed; no lint script is configured
- `npm run build` - core and CLI compiled, app build failed in Vite with `spawn EPERM`
- `npm test` - failed because Node's test runner hit `spawn EPERM`
- `node packages/core/dist/context.test.js` - passed
- `node packages/core/dist/scanner.test.js` - passed
- `node packages/core/dist/treeBuilder.test.js` - passed
- `node packages/core/dist/validator.test.js` - passed
- `npm run atree:scan` - passed
- `npm run atree:validate` - passed
- `git diff --stat -- README.md docs packages/core/src packages/cli/src .abstraction-tree` after scan

## What Improved

`docs/AGENT_PROTOCOL.md` now gives future agents a concrete bounded-loop preflight: check current diff, loop state, latest run reports, and latest lessons before broad repository exploration.

## What Did Not Improve

No validator, scanner, context-pack scoring, CLI behavior, or app behavior changed.

## Mistakes / Risks

The canonical full build and `npm test` commands remain blocked by sandbox subprocess restrictions. The fallback direct compiled core tests passed, but they are not a full replacement for the canonical commands.

Because this loop ran on an already dirty tree, the final diff against `HEAD` includes earlier validator and abstraction-memory changes in addition to this loop's protocol documentation.

## Missing Context Discovered

No context packs were present under `.abstraction-tree/context-packs/`. Recent reports and lessons were sufficient to choose a small docs improvement without broad repository exploration.

## Tree Updates Needed

No further tree updates are needed. `npm run atree:scan` refreshed root abstraction memory and `npm run atree:validate` passed.

## Reusable Lesson

Self-improvement loops should begin from existing loop state, recent reports, and recent lessons before broad repository search.

## Recommended Next Loop

Add a focused validation check for duplicate ontology level names, unless the validator files are still too dirty to extend safely.

## Final Instruction

Complete exactly one loop, then stop.
