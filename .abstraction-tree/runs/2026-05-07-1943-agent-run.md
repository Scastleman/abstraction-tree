# Agent Run Report

## Task Chosen

Add evaluation metrics and a warning for generated scan change-record buildup.

## Why This Task

The previous loop recommended defining a retention or review policy for generated scan change records so autopilot work can stay within the diff-size guard. The hypothesis was that evaluation becomes more useful if it separates generated scan records from semantic change records and flags when generated records need review.

## Files Changed

Changed by this loop:

- `packages/core/src/evaluator.ts`
- `packages/core/src/evaluator.test.ts`
- `.abstraction-tree/changes/evaluator-scan-record-review.2026-05-07-1942.json`
- `.abstraction-tree/changes/scan.1778197374198.json`
- `.abstraction-tree/evaluations/2026-05-07-1943-evaluation.json`
- `.abstraction-tree/lessons/2026-05-07-1943-lesson.md`
- `.abstraction-tree/runs/2026-05-07-1943-agent-run.md`
- `.abstraction-tree/`

Pre-existing dirty files and generated memory artifacts from earlier autopilot loops were already present across core, CLI, app, package metadata, scripts, and `.abstraction-tree/`. They were not reverted.

## Abstraction Layer Affected

tests

## Result

success

## Checks Run

- `npm test` before rebuild: 69/69, but did not include the new TypeScript test because built test artifacts were stale
- `npm run build`
- `npm test`: 70/70
- `npm run format:check`
- `npm run check:unicode`
- `npm run atree -- scan --project .`
- `npm run atree -- validate --project . --strict`
- `npm run atree -- evaluate --project .` (wrote `2026-05-07-1943-evaluation.json`)
- `npm run diff:summary`

## What Improved

Evaluation reports a new `changes` section with total change records, generated scan records, semantic change records, and a `generatedScanReviewNeeded` flag. The current evaluation measured 82 total change records, 69 generated scan records, 13 semantic records, and emitted one changes warning.

## What Did Not Improve

This loop did not delete, archive, or consolidate any generated scan records. The overall working tree still exceeds the diff guard because earlier autopilot loops left many source and memory changes uncommitted.

## Mistakes / Risks

The first test run passed without exercising the new test because the runner imports built package tests from `packages/core/dist`. Rebuilding before testing fixed the verification gap. The new evaluation warning is intentionally non-blocking, so future agents still need to act on it.

## Missing Context Discovered

None beyond the already-known issue that the current autopilot worktree includes many pre-existing generated memory files.

## Tree Updates Needed

None remaining. The scan refreshed abstraction memory and strict validation passed.

## Reusable Lesson

When a repo test runner executes built artifacts, rebuild before trusting a new TypeScript test. Otherwise the suite can pass while newly added source tests are still absent from execution.

## Recommended Next Loop

Add a non-destructive review command or report that lists generated scan records eligible for consolidation.
