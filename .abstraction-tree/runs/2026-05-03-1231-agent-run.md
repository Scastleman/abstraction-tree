# Agent Run Report

## Task Chosen

Add validation for tree nodes that reference missing invariant IDs.

## Why This Task

The latest prior run recommended checking whether `tree.json` node `invariants` entries still exist in `invariants.json`. These back-references guide context packs and agent boundaries, so stale IDs can make future agents trust non-existent rules.

## Files Changed

- `packages/core/src/validator.ts`
- `packages/core/src/validator.test.ts`
- `.abstraction-tree/concepts.json`
- `.abstraction-tree/files.json`
- `.abstraction-tree/invariants.json`
- `.abstraction-tree/tree.json`
- `.abstraction-tree/changes/scan.1777825853744.json`
- `.abstraction-tree/runs/2026-05-03-1231-agent-run.md`
- `.abstraction-tree/lessons/2026-05-03-1231-lesson.md`
- `.abstraction-tree/automation/loop-state.json`

The worktree was already dirty before this loop from prior validation loops, including validator/CLI edits, regenerated abstraction-memory files, scan records, and prior run/lesson files. This loop preserved that work and added only tree-node invariant back-reference validation plus this loop's required records.

## Abstraction Layer Affected

schema

## Result

partial

## Checks Run

- `git diff --stat` before editing
- Targeted reads of `README.md`, `docs/`, `packages/core/src/`, `packages/cli/src/`, `.abstraction-tree/`, latest prior run reports, and latest prior lessons
- `git diff -- packages/core/src/validator.ts packages/core/src/validator.test.ts`
- `npm run lint --if-present` - passed; no lint script is configured
- `npm run build` - core and CLI compiled, app build failed in Vite with `spawn EPERM`
- `npm test` - failed because Node's test runner hit `spawn EPERM`
- `node packages/core/dist/validator.test.js` - passed
- `node packages/core/dist/context.test.js` - passed
- `node packages/core/dist/scanner.test.js` - passed
- `node packages/core/dist/treeBuilder.test.js` - passed
- `npm run atree:scan` - passed
- `npm run atree:validate` - passed
- `git diff --stat -- README.md docs packages/core/src packages/cli/src .abstraction-tree` after scan

## What Improved

`validateInvariants` now reports any tree node whose `invariants` array names an invariant ID that is absent from `invariants.json`. A focused validator test covers the missing back-reference case, and the existing CLI validation path picks it up because it already passes nodes into `validateInvariants`.

## What Did Not Improve

Validation still does not check concept `relatedNodeIds` or `relatedFiles`, tree node confidence ranges, malformed change records, or whether invariant forward references and node back-references are symmetrical.

## Mistakes / Risks

The canonical full build and `npm test` commands remain blocked by sandbox subprocess restrictions. Direct compiled core tests passed, but they are not a full replacement for the canonical commands.

Because this loop built on an already dirty validator, CLI, and abstraction-memory state, the final diff against `HEAD` includes prior validation loops as well as this tree-node invariant reference validation change.

## Missing Context Discovered

Per-loop provenance cannot be recovered from `git diff` alone while multiple prior loops remain uncommitted; recent run reports are necessary to distinguish current edits from pre-existing dirty files.

## Tree Updates Needed

No further tree updates are needed. `npm run atree:scan` refreshed root abstraction memory and `npm run atree:validate` passed.

## Reusable Lesson

Tree node `invariants` entries are schema back-references and should be validated against `invariants.json`, not only trusted as generated tree metadata.

## Recommended Next Loop

Add focused validation for concept references, such as `relatedNodeIds` missing from `tree.json` or `relatedFiles` missing from `files.json`.

## Final Instruction

Complete exactly one loop, then stop.
