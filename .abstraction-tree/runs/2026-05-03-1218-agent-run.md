# Agent Run Report

## Task Chosen

Add duplicate concept ID validation.

## Why This Task

The latest prior run recommended checking duplicate concept IDs or invariant IDs. Concept IDs are used by context pack generation for de-duplication, so duplicate IDs can silently hide concept context that future agents need.

## Files Changed

- `packages/core/src/validator.ts`
- `packages/core/src/validator.test.ts`
- `packages/cli/src/index.ts`
- `.abstraction-tree/concepts.json`
- `.abstraction-tree/files.json`
- `.abstraction-tree/invariants.json`
- `.abstraction-tree/tree.json`
- `.abstraction-tree/changes/scan.1777825147253.json`
- `.abstraction-tree/runs/2026-05-03-1218-agent-run.md`
- `.abstraction-tree/lessons/2026-05-03-1218-lesson.md`
- `.abstraction-tree/automation/loop-state.json`

The worktree was already dirty before this loop from the 2026-05-03 12:15 ontology confidence loop, including validator changes and regenerated abstraction-memory files. This loop preserved that work and added only concept ID validation plus the CLI hook.

## Abstraction Layer Affected

schema

## Result

partial

## Checks Run

- `git diff --stat; git diff -- .abstraction-tree README.md docs packages/core/src packages/cli/src` before editing
- Targeted reads of `README.md`, `docs/`, `packages/core/src/`, `packages/cli/src/`, `.abstraction-tree/`, prior run reports, and prior lessons
- `git diff -- packages/core/src/validator.ts packages/core/src/validator.test.ts packages/cli/src/index.ts`
- `npm run lint --if-present` - passed; no lint script is configured
- `npm run build` - core and CLI compiled, app build failed in Vite with `spawn EPERM`
- `npm test` - failed because Node's test runner hit `spawn EPERM`
- `node packages/core/dist/validator.test.js` - passed
- `node packages/core/dist/context.test.js` - passed
- `node packages/core/dist/scanner.test.js` - passed
- `node packages/core/dist/treeBuilder.test.js` - passed
- `npm run atree:scan` - passed
- `npm run atree:validate` - passed
- `git diff --stat -- README.md docs packages/core/src packages/cli/src .abstraction-tree` after scan

## What Improved

Core now exposes `validateConcepts`, which reports duplicate concept IDs before downstream context pack de-duplication can collapse them. The CLI `validate` command now reads `concepts.json` and includes those issues in normal strict validation. A focused test covers the duplicate ID case.

## What Did Not Improve

Validation still does not check duplicate invariant IDs, malformed concept references, malformed invariant references, tree node confidence ranges, or change record schema shape.

## Mistakes / Risks

The canonical full build and `npm test` commands remain blocked by sandbox subprocess restrictions. The fallback direct compiled core tests passed, but they are not a full replacement for the canonical commands.

Because this loop built on an already dirty validator and abstraction-memory state, the final diff against `HEAD` includes the prior rank-shape and confidence validation loops as well as this concept ID validation change.

## Missing Context Discovered

No context packs were present under `.abstraction-tree/context-packs/`. Recent reports and lessons were enough to choose the validation task without broad repository exploration.

## Tree Updates Needed

No further tree updates are needed. `npm run atree:scan` refreshed root abstraction memory and `npm run atree:validate` passed.

## Reusable Lesson

Concept IDs should be validated before context pack generation because context packs de-duplicate concepts by ID.

## Recommended Next Loop

Add focused validation for duplicate invariant IDs, since invariants are also consumed through ID references from tree nodes and context packs.

## Final Instruction

Complete exactly one loop, then stop.
