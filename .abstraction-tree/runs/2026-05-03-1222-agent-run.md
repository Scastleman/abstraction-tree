# Agent Run Report

## Task Chosen

Add duplicate invariant ID validation.

## Why This Task

The latest prior run recommended checking duplicate invariant IDs. Invariants are referenced from tree nodes and context packs, so duplicate IDs can make agent boundaries ambiguous or cause future ID-keyed consumers to collapse distinct rules.

## Files Changed

- `packages/core/src/validator.ts`
- `packages/core/src/validator.test.ts`
- `packages/cli/src/index.ts`
- `.abstraction-tree/concepts.json`
- `.abstraction-tree/files.json`
- `.abstraction-tree/invariants.json`
- `.abstraction-tree/tree.json`
- `.abstraction-tree/changes/scan.1777825346897.json`
- `.abstraction-tree/runs/2026-05-03-1222-agent-run.md`
- `.abstraction-tree/lessons/2026-05-03-1222-lesson.md`
- `.abstraction-tree/automation/loop-state.json`

The worktree was already dirty before this loop from the 2026-05-03 12:18 duplicate concept ID validation loop, including validator/CLI changes, regenerated abstraction-memory files, scan records, and prior run/lesson files. This loop preserved that work and added only duplicate invariant ID validation plus this loop's required records.

## Abstraction Layer Affected

schema

## Result

partial

## Checks Run

- `git diff -- README.md docs packages/core/src packages/cli/src .abstraction-tree` before editing
- Targeted reads of `README.md`, `docs/`, `packages/core/src/`, `packages/cli/src/`, `.abstraction-tree/`, prior run reports, and prior lessons
- `git diff -- packages/core/src/validator.ts packages/core/src/validator.test.ts packages/cli/src/index.ts`
- `npm run lint --if-present` - passed; no lint script is configured
- `npm run build` - core and CLI compiled, app build failed in Vite with `spawn EPERM`
- `npm test` - failed because Node's test runner hit `spawn EPERM`
- `node packages/core/dist/validator.test.js` - passed
- `node packages/core/dist/context.test.js` - passed
- `node packages/core/dist/scanner.test.js` - passed
- `node packages/core/dist/treeBuilder.test.js` - passed
- `npm run atree:scan` - passed
- `npm run atree:validate` - passed
- `git diff --stat -- README.md docs packages/core/src packages/cli/src .abstraction-tree` after scan

## What Improved

Core now exposes `validateInvariants`, which reports duplicate invariant IDs before downstream references or ID-keyed consumers can collapse distinct rules. The CLI `validate` command now reads `invariants.json` and includes those issues in normal strict validation. A focused test covers the duplicate invariant ID case.

## What Did Not Improve

Validation still does not check malformed invariant references, malformed concept references, tree node confidence ranges, or change record schema shape.

## Mistakes / Risks

The canonical full build and `npm test` commands remain blocked by sandbox subprocess restrictions. The fallback direct compiled core tests passed, but they are not a full replacement for the canonical commands.

Because this loop built on an already dirty validator, CLI, and abstraction-memory state, the final diff against `HEAD` includes prior validation loops as well as this invariant ID validation change.

## Missing Context Discovered

No context packs were present under `.abstraction-tree/context-packs/`. Recent reports and lessons were enough to choose the task without broad repository exploration.

## Tree Updates Needed

No further tree updates are needed. `npm run atree:scan` refreshed root abstraction memory and `npm run atree:validate` passed.

## Reusable Lesson

Invariant IDs should be unique before validation or context-pack use because duplicate IDs can make tree references and agent boundaries ambiguous.

## Recommended Next Loop

Add focused validation for malformed invariant references, such as invariant `nodeIds` that do not exist in `tree.json` or `filePaths` that do not exist in `files.json`.

## Final Instruction

Complete exactly one loop, then stop.
