# Lesson

When validating tree structure, checking that referenced IDs exist is not enough. Parent pointers and child lists should be verified as a bidirectional contract so stale or contradictory abstraction memory fails validation early.
