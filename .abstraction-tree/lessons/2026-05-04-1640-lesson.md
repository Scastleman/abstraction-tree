# Lesson

Final-pass docs should name both sides of the memory boundary: commit durable abstraction memory, stable automation config, run reports, lessons, and evaluations; keep live counters, local loop state, secrets, logs, and local Codex state out of version control. Pair narrative run reports with deterministic metrics so future agents can verify drift, ownership, context breadth, and automation health instead of relying only on self-reporting.
