# Lesson

Hypothesis-driven autonomous loops need objective guardrails in both prompt and runner. Keep policy in committed config, keep counters in ignored runtime state, validate the committed runtime template, and gate any automatic commit behind build, tests, and validation.

Evidence: build passed, focused automation validation tests passed 7/7, strict validation passed after memory refresh, and evaluation reported 0 issues with 0 stale or missing files. Full `npm test` was blocked by sandbox `spawn EPERM`, so direct-file test execution remains the practical fallback here.
