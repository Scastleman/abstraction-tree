# Lesson

Ontology validators should check level ID uniqueness before building ID-keyed lookup sets. Duplicate ontology IDs can otherwise collapse into one lookup entry and hide contradictory abstraction-layer definitions.
