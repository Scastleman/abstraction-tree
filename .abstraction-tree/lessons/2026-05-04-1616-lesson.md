# Lesson

Dependency-free hygiene checks are safer than unverified formatter dependencies in an offline mission loop.

When network access is unavailable, do not wire CI to a package that cannot be installed and tested. Use a narrow local checker, document what it does not cover, and leave a clear path to adopt the stronger formatter once the dependency can be locked and verified.
