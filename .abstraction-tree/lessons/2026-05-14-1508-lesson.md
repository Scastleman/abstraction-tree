# Lesson

Beta readiness needs explicit dist-tag handling. A prerelease dry run can pass local package smoke tests but still fail npm publish validation unless the release tooling adds `--tag beta` for beta versions or another intentional prerelease tag for release candidates.
