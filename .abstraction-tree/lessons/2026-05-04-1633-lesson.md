# Lesson

Self-dogfooding UI panels are safest when they reuse existing deterministic repo artifacts, expose missing data as unknown, and avoid adding new runtime dependencies or model calls.

When a visual app reflects project health, include stale-memory validation in the checks because the UI can otherwise present current-looking data backed by outdated abstraction memory.
