# Lesson

Tree node `confidence` values should be validated as finite numbers from `0` to `1`. Persisted `tree.json` data can be hand-edited or provider-generated, so runtime validation should not trust the TypeScript schema alone.
