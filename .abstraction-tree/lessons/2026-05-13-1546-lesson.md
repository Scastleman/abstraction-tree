# Lesson

Tree readability needs fields separate from compact summaries. Keep `summary` short for compatibility, search, and dense UI surfaces, then generate `explanation` from deterministic evidence so humans and agents can understand role, ownership, dependencies, invariants, and safe-change guidance without widening scope unnecessarily. For parent nodes, also generate `separationLogic` so the tree describes the child partition rule and which lower boundary should be chosen before editing.
