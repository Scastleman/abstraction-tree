# Lesson

Concept `relatedNodeIds` and `relatedFiles` directly influence context-pack relevance. Validate them against current tree nodes and known files so stale concept memory does not mislead future agents.
