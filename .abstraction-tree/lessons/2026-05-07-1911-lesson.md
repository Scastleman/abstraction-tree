# Lesson

UI readers of tree metadata need the same compatibility fallback contract as core readers. When `sourceFiles` is present but empty, app search and file display should still inspect legacy `ownedFiles` so alias-shaped nodes remain discoverable.
