# Lesson

Compatibility fallback rules should be treated as a shared contract across all tree readers. If `sourceFiles` is present but empty, validators must still inspect legacy `ownedFiles`; otherwise stale ownership can pass validation while context and evaluation report different behavior.
