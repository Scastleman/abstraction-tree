# Lesson

Change records are persisted JSON, not trusted TypeScript objects. Validate their runtime shape before iterating reference fields so malformed arrays do not create misleading validation output.
