# Lesson

When a small package metadata fix changes a tracked file, strict abstraction-tree validation may fail until `npm run atree:scan` refreshes file hashes. Treat the scan as part of the fix when the source change is intentional and the memory diff is limited to updated file summaries plus a scan record.
