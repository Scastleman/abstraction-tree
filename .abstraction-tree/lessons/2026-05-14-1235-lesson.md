# Lesson

V1 productization needs executable proof, not just documentation. The stable path should be tested through packed installs and fixture projects, and docs should only advertise commands that exist and pass smoke checks.
