# Lesson

Invariant `filePaths` can point at existing project memory files that are intentionally omitted from `files.json`. Validate those paths against scanned files plus known existing ignored files to avoid false positives.
