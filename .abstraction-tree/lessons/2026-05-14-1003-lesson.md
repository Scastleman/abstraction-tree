# Lesson

Visual adoption features should guide users from setup to inspection without taking control by default. An explicit `--open` flag makes the happy path easy while keeping CI, remote terminals, and tests deterministic.

When setup guidance changes, test the reusable message formatter rather than only the end-to-end command. That keeps CLI copy intentional and prevents the product flow from drifting out of the docs.
