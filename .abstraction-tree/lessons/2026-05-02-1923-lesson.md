# Lesson

File memory validators should check path uniqueness before building path-keyed `Set` or `Map` lookups. Duplicate paths can otherwise collapse into a single entry and hide stale or contradictory file summaries.
