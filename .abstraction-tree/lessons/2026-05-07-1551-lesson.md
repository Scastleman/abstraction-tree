# Lesson

When compatibility aliases exist in the abstraction tree schema, helper functions should share the same fallback semantics across readers. In this case, context-pack scoring needed the same non-empty `sourceFiles` before `ownedFiles` behavior already used by evaluator drift checks.
