# Lesson

When one component generates data specifically to satisfy another component's heuristic, keep both sides on shared named limits. Separate magic numbers make future threshold changes easy to apply on only one side, which can reintroduce evaluation noise without changing behavior intentionally.
