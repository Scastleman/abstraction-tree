# Lesson

Ontology ranks should be non-negative integers and contiguous from `0`. Unique ranks alone do not guarantee that abstraction layers have a deterministic root-to-leaf order for agents and UI consumers.
