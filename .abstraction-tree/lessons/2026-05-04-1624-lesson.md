# Lesson

Define LLM-assisted abstraction as validated proposals, not direct memory writes.

Provider-neutral interfaces can live in core, but provider adapters should remain outside the deterministic scanner, tree builder, and CLI default path.
