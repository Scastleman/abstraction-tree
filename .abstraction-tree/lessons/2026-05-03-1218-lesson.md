# Lesson

Concept IDs should be unique before context pack generation runs. Context packs de-duplicate concepts by `id`, so validation should catch duplicates before agents lose related files or node links.
