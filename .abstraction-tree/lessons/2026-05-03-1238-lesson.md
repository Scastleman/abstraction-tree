# Lesson

Change records are context-pack inputs, not passive logs. Validate `affectedNodeIds`, `filesChanged`, and `invariantsPreserved` against current memory so stale semantic history does not mislead future agents.
