# Lesson

Committed automation policy and local runtime counters should be separate files. A committed runtime example gives agents a reproducible starting shape, while the ignored runtime file preserves local counters without putting loop progress or stop flags into source control.
