# Lesson

Generated scan records can be consolidated, but cleanup must preserve any scan files referenced by semantic change records. The safe pattern is to keep the latest generated scan, keep referenced generated scans, prune only unreferenced superseded scans, then validate the tree and change log before treating the cleanup as complete.
