# Lesson

Tree validators should check identifier uniqueness before building `Set` or `Map` lookups. Lookup structures are useful for later validation, but they can hide duplicate source records by keeping only one value per key.
