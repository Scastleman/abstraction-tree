# Lesson

Tree validation should follow parent chains to detect cycles. A detached cycle can satisfy bidirectional parent/children checks and still be unreachable from the root, so root-count validation alone is not enough.
