# Lesson

When Git-backed file listing is unavailable, formatter and Unicode hygiene fallbacks still need to respect ignored runtime-state boundaries. Otherwise local files like `.abstraction-tree/automation/mission-runtime.json` can fail release checks even though they are intentionally uncommitted.
