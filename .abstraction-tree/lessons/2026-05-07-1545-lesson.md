# Lesson

When a repository adds BOM tolerance to a shared JSON loader, search for remaining direct `JSON.parse(await readFile(...))` or raw `JSON.parse(raw)` paths. Otherwise validation and serving code can disagree about whether the same project-local JSON file is valid.
