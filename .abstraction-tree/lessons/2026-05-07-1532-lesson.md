# Lesson

When generated status reports and automation state become part of a UI-facing health surface, test the served API path, not only the underlying validators. That path can expose integration issues such as heading drift or BOM-prefixed JSON that unit-level checks may miss.
