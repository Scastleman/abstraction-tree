When a mission bundle defines phases, execute one phase at a time and run `atree:evaluate` after the batch. Passing tests are not enough; generated memory can drift when new concepts compete for a fixed concept budget.
