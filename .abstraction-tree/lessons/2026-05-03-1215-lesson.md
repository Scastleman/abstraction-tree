# Lesson

Ontology confidence values should be finite numbers between `0` and `1`. Generated defaults may be valid today, but validation should still protect future agents from hand-edited or provider-generated confidence values that cannot be compared reliably.
