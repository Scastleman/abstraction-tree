# Lesson

Context-pack builder caps should stay below evaluator over-breadth thresholds. If the builder can emit packs at or above those limits, normal context generation can create evaluation noise even when the scoring logic is working as designed.
