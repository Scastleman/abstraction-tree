# Lesson

Ontology validation should check normalized human-readable level names, not only IDs and ranks. Duplicate layer names can confuse future agents and UI consumers even when machine IDs are unique.
