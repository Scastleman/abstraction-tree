# Lesson

Bounded self-improvement loops should start from `git diff`, loop state, recent run reports, and recent lessons before broad repository search. This keeps token use low and makes pre-existing dirty work visible before choosing a change.
